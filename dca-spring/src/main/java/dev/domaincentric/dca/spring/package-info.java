/**
 * Spring implementations of the runtime contracts in {@code dca-building-blocks}: the {@code
 * DomainEventPublisher} output port and the {@code TransactionBoundary} execution abstraction. The
 * building blocks stay framework-free; this artifact is where Spring is allowed to appear.
 */
package dev.domaincentric.dca.spring;
