package dev.domaincentric.dca.archunit.fixtures.features.depth.flat.ordering.application.support;

/** Sits one level deeper than the flat use cases; ignored because it is abstract. */
public abstract class BaseUseCase {}
