/** Declared with a second module system's annotation, not with the first configured one. */
@BoundedContext(name = "Ledger", description = "Postings")
@Module
package dev.domaincentric.dca.archunit.fixtures.frameworks.modules.ledger;

import dev.domaincentric.dca.buildingblocks.ddd.strategic.BoundedContext;
import org.example.modules.Module;
