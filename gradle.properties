org.gradle.caching=true
org.gradle.parallel=true

# Artifact versions. Independent per artifact; the release workflow overrides the one it publishes
# (tag building-blocks/vX.Y.Z or archunit/vX.Y.Z). Keep buildingBlocksVersion at the latest released
# version — dca-archunit's POM depends on exactly this version.
buildingBlocksVersion=0.1.1
archunitVersion=0.2.0-SNAPSHOT
